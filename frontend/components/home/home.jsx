import React from 'react';
import StreamContainer from '../stream/stream_container';

const Home = ({ children }) => (
  <main className="home">
    <StreamContainer />
  </main>
);

export default Home;
